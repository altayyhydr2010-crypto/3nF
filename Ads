-- =========================================== --
-- Script: TimeBomb Ultimate V3
-- Developer: 3nF
-- Executor: Delta
-- Telegram: xxx9ooo
-- ===========================================

local player = game.Players.LocalPlayer

-- حذف الواجهة القديمة
pcall(function()
    player.PlayerGui:FindFirstChild("3nF_TimeBomb"):Destroy()
end)

-- =========================================== --
-- إنشاء الواجهة الرئيسية
-- =========================================== --
local gui = Instance.new("ScreenGui")
gui.Name = "3nF_TimeBomb"
gui.Parent = player.PlayerGui
gui.ResetOnSpawn = false

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 360, 0, 480)
frame.Position = UDim2.new(0.5, -180, 0.5, -240)
frame.BackgroundColor3 = Color3.fromRGB(10, 10, 28)
frame.BackgroundTransparency = 0.08
frame.BorderSizePixel = 3
frame.BorderColor3 = Color3.fromRGB(255, 180, 50)
frame.ClipsDescendants = true
frame.Parent = gui

-- =========================================== --
-- شريط العنوان (منطقة السحب)
-- =========================================== --
local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1, 0, 0, 45)
titleBar.BackgroundColor3 = Color3.fromRGB(20, 20, 50)
titleBar.BorderSizePixel = 0
titleBar.Parent = frame

local title = Instance.new("TextLabel")
title.Size = UDim2.new(0.6, 0, 1, 0)
title.Position = UDim2.new(0.05, 0, 0, 0)
title.BackgroundTransparency = 1
title.Text = "💣 TIMEBOMB V3"
title.TextColor3 = Color3.fromRGB(255, 200, 50)
title.TextScaled = true
title.Font = Enum.Font.GothamBold
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = titleBar

-- زر الإخفاء (Hide UI) - يخفي كل المحتوى
local hideBtn = Instance.new("TextButton")
hideBtn.Size = UDim2.new(0, 75, 0, 30)
hideBtn.Position = UDim2.new(0.62, 0, 0, 7)
hideBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 90)
hideBtn.BorderSizePixel = 0
hideBtn.Text = "👁️ Hide"
hideBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
hideBtn.TextScaled = true
hideBtn.Font = Enum.Font.GothamBold
hideBtn.Parent = titleBar

local uiVisible = true
hideBtn.MouseButton1Click:Connect(function()
    uiVisible = not uiVisible
    for _, child in pairs(frame:GetChildren()) do
        if child ~= titleBar then
            child.Visible = uiVisible
        end
    end
    hideBtn.Text = uiVisible and "👁️ Hide" or "👁️ Show"
end)

-- زر الإغلاق
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 30, 0, 30)
closeBtn.Position = UDim2.new(1, -38, 0, 7)
closeBtn.BackgroundColor3 = Color3.fromRGB(200, 30, 30)
closeBtn.BorderSizePixel = 0
closeBtn.Text = "✕"
closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
closeBtn.TextScaled = true
closeBtn.Font = Enum.Font.GothamBold
closeBtn.Parent = titleBar

closeBtn.MouseButton1Click:Connect(function()
    frame.Visible = not frame.Visible
end)

-- =========================================== --
-- تحريك القائمة (من شريط العنوان)
-- =========================================== --
local dragging = false
local dragStart, startPos

titleBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = frame.Position
    end
end)

titleBar.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

game:GetService("UserInputService").InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        frame.Position = UDim2.new(
            startPos.X.Scale,
            startPos.X.Offset + delta.X,
            startPos.Y.Scale,
            startPos.Y.Offset + delta.Y
        )
    end
end)

-- اختصار F5
game:GetService("UserInputService").InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.F5 then
        frame.Visible = not frame.Visible
    end
end)

-- =========================================== --
-- دوال إنشاء الأزرار
-- =========================================== --
local function createToggle(text, yPos, color, callback)
    local f = Instance.new("Frame")
    f.Size = UDim2.new(0.92, 0, 0, 32)
    f.Position = UDim2.new(0.04, 0, yPos, 0)
    f.BackgroundColor3 = Color3.fromRGB(18, 18, 42)
    f.BorderSizePixel = 1
    f.BorderColor3 = Color3.fromRGB(50, 50, 90)
    f.Parent = frame

    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(0.5, 0, 1, 0)
    lbl.Position = UDim2.new(0.03, 0, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = text
    lbl.TextColor3 = Color3.fromRGB(220, 220, 230)
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.TextScaled = true
    lbl.Font = Enum.Font.Gotham
    lbl.Parent = f

    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 60, 0.8, 0)
    btn.Position = UDim2.new(0.7, 0, 0.1, 0)
    btn.BackgroundColor3 = Color3.fromRGB(50, 50, 80)
    btn.BorderSizePixel = 2
    btn.BorderColor3 = color or Color3.fromRGB(255, 170, 50)
    btn.Text = "OFF"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextScaled = true
    btn.Font = Enum.Font.GothamBold
    btn.Parent = f

    local state = false
    btn.MouseButton1Click:Connect(function()
        state = not state
        btn.Text = state and "ON" or "OFF"
        btn.BackgroundColor3 = state and Color3.fromRGB(0, 200, 80) or Color3.fromRGB(50, 50, 80)
        if callback then callback(state) end
    end)

    return btn, f
end

local function createSlider(text, yPos, min, max, default, callback)
    local f = Instance.new("Frame")
    f.Size = UDim2.new(0.92, 0, 0, 32)
    f.Position = UDim2.new(0.04, 0, yPos, 0)
    f.BackgroundColor3 = Color3.fromRGB(18, 18, 42)
    f.BorderSizePixel = 1
    f.BorderColor3 = Color3.fromRGB(50, 50, 90)
    f.Parent = frame

    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(0.5, 0, 1, 0)
    lbl.Position = UDim2.new(0.03, 0, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = text .. " " .. tostring(default) .. "%"
    lbl.TextColor3 = Color3.fromRGB(220, 220, 230)
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.TextScaled = true
    lbl.Font = Enum.Font.Gotham
    lbl.Parent = f

    local slider = Instance.new("Frame")
    slider.Size = UDim2.new(0.35, 0, 0.5, 0)
    slider.Position = UDim2.new(0.58, 0, 0.25, 0)
    slider.BackgroundColor3 = Color3.fromRGB(40, 40, 70)
    slider.BorderSizePixel = 1
    slider.BorderColor3 = Color3.fromRGB(255, 170, 50)
    slider.Parent = f

    local fill = Instance.new("Frame")
    fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
    fill.BackgroundColor3 = Color3.fromRGB(255, 170, 50)
    fill.BorderSizePixel = 0
    fill.Parent = slider

    local value = default
    local draggingSlider = false

    slider.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            draggingSlider = true
        end
    end)

    slider.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            draggingSlider = false
        end
    end)

    slider.MouseMoved:Connect(function()
        if draggingSlider then
            local mouse = player:GetMouse()
            local relX = (mouse.X - slider.AbsolutePosition.X) / slider.AbsoluteSize.X
            local clamped = math.clamp(relX, 0, 1)
            fill.Size = UDim2.new(clamped, 0, 1, 0)
            value = math.floor(min + clamped * (max - min))
            lbl.Text = text .. " " .. tostring(value) .. "%"
            if callback then callback(value) end
        end
    end)

    return fill, f
end

-- =========================================== --
-- إنشاء المميزات (خاصة بـ TimeBomb)
-- =========================================== --
local y = 0.10

-- عنوان GENERAL
local genLabel = Instance.new("TextLabel")
genLabel.Size = UDim2.new(0.92, 0, 0, 22)
genLabel.Position = UDim2.new(0.04, 0, y, 0)
genLabel.BackgroundColor3 = Color3.fromRGB(255, 170, 50)
genLabel.BackgroundTransparency = 0.2
genLabel.Text = " ⚙️ GENERAL"
genLabel.TextColor3 = Color3.fromRGB(255, 200, 100)
genLabel.TextXAlignment = Enum.TextXAlignment.Left
genLabel.TextScaled = true
genLabel.Font = Enum.Font.GothamBold
genLabel.Parent = frame
y = y + 0.07

createToggle("⚡ Auto Pass", y, Color3.fromRGB(255, 170, 50), function(state) print("Auto Pass: "..tostring(state)) end)
y = y + 0.085
createToggle("🔄 Auto Spam", y, Color3.fromRGB(255, 150, 50), function(state) print("Auto Spam: "..tostring(state)) end)
y = y + 0.085
createToggle("💤 AFK Mode", y, Color3.fromRGB(200, 200, 50), function(state) print("AFK: "..tostring(state)) end)
y = y + 0.085
createSlider("🎯 Accuracy", y, 0, 100, 80, function(v) print("Accuracy: "..v) end)
y = y + 0.085
createSlider("⚡ Speed", y, 0, 100, 50, function(v) print("Speed: "..v) end)
y = y + 0.085

-- عنوان COMBAT
local combatLabel = Instance.new("TextLabel")
combatLabel.Size = UDim2.new(0.92, 0, 0, 22)
combatLabel.Position = UDim2.new(0.04, 0, y, 0)
combatLabel.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
combatLabel.BackgroundTransparency = 0.2
combatLabel.Text = " ⚔️ COMBAT"
combatLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
combatLabel.TextXAlignment = Enum.TextXAlignment.Left
combatLabel.TextScaled = true
combatLabel.Font = Enum.Font.GothamBold
combatLabel.Parent = frame
y = y + 0.07

createToggle("🛡️ God Mode", y, Color3.fromRGB(50, 255, 150), function(state) print("God Mode: "..tostring(state)) end)
y = y + 0.085
createToggle("🖐️ Reach", y, Color3.fromRGB(200, 100, 255), function(state) print("Reach: "..tostring(state)) end)
y = y + 0.085
createToggle("🔄 Auto Win", y, Color3.fromRGB(80, 200, 80), function(state) print("Auto Win: "..tostring(state)) end)
y = y + 0.085

-- عنوان EXTRA
local extraLabel = Instance.new("TextLabel")
extraLabel.Size = UDim2.new(0.92, 0, 0, 22)
extraLabel.Position = UDim2.new(0.04, 0, y, 0)
extraLabel.BackgroundColor3 = Color3.fromRGB(100, 50, 200)
extraLabel.BackgroundTransparency = 0.2
extraLabel.Text = " 🔧 EXTRA"
extraLabel.TextColor3 = Color3.fromRGB(180, 100, 255)
extraLabel.TextXAlignment = Enum.TextXAlignment.Left
extraLabel.TextScaled = true
extraLabel.Font = Enum.Font.GothamBold
extraLabel.Parent = frame
y = y + 0.07

createToggle("👁️ ESP", y, Color3.fromRGB(255, 100, 200), function(state) print("ESP: "..tostring(state)) end)
y = y + 0.085
createToggle("🔒 Anti-Crash", y, Color3.fromRGB(255, 80, 120), function(state) print("Anti-Crash: "..tostring(state)) end)
y = y + 0.085
createToggle("💥 Explosion", y, Color3.fromRGB(255, 150, 50), function(state) print("Explosion: "..tostring(state)) end)
y = y + 0.085

-- =========================================== --
-- زر STOP ALL
-- =========================================== --
local stopBtn = Instance.new("TextButton")
stopBtn.Size = UDim2.new(0.3, 0, 0, 32)
stopBtn.Position = UDim2.new(0.35, 0, 0.96, 0)
stopBtn.BackgroundColor3 = Color3.fromRGB(200, 30, 30)
stopBtn.BorderSizePixel = 2
stopBtn.BorderColor3 = Color3.fromRGB(255, 80, 80)
stopBtn.Text = "🛑 STOP ALL"
stopBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
stopBtn.TextScaled = true
stopBtn.Font = Enum.Font.GothamBold
stopBtn.Parent = frame

stopBtn.MouseButton1Click:Connect(function()
    for _, child in pairs(frame:GetChildren()) do
        if child:IsA("Frame") and child:FindFirstChild("TextButton") then
            local btn = child:FindFirstChild("TextButton")
            if btn and btn.Text == "ON" then
                btn.Text = "OFF"
                btn.BackgroundColor3 = Color3.fromRGB(50, 50, 80)
            end
        end
    end
    local char = player.Character
    if char then
        local humanoid = char:FindFirstChild("Humanoid")
        if humanoid then
            humanoid.WalkSpeed = 16
            humanoid.MaxHealth = 100
            humanoid.Health = 100
        end
    end
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "3nF Hub",
        Text = "🛑 All features stopped!",
        Duration = 2
    })
end)

-- =========================================== --
-- اسم الصانع والتليجرام
-- =========================================== --
local credit = Instance.new("TextLabel")
credit.Size = UDim2.new(1, 0, 0, 16)
credit.Position = UDim2.new(0, 0, 1.02, 0)
credit.BackgroundTransparency = 1
credit.Text = "⭐ Script by 3nF ⭐"
credit.TextColor3 = Color3.fromRGB(255, 170, 50)
credit.TextScaled = true
credit.Font = Enum.Font.Gotham
credit.Parent = frame

local telegram = Instance.new("TextLabel")
telegram.Size = UDim2.new(1, 0, 0, 16)
telegram.Position = UDim2.new(0, 0, 1.06, 0)
telegram.BackgroundTransparency = 1
telegram.Text = "📱 Telegram: xxx9ooo"
telegram.TextColor3 = Color3.fromRGB(120, 200, 255)
telegram.TextScaled = true
telegram.Font = Enum.Font.Gotham
telegram.Parent = frame

-- =========================================== --
-- إشعار التشغيل
-- =========================================== --
print("✅ TimeBomb Ultimate V3 Loaded! | Script by 3nF")
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "💣 3nF TimeBomb",
    Text = "Ultimate V3 Loaded! | Drag to move | F5 to hide",
    Duration = 4
})
